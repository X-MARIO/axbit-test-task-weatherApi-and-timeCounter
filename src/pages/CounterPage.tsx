import React from 'react';
import Counter from '../components/Counter/Counter';

const CounterPage: React.FC = (): React.ReactElement => {
    return (
        <Counter/>
    );
};

export default CounterPage;